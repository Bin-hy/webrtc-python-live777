import './index'

window.addEventListener('DOMContentLoaded', () => {
    HTMLDivElement;
    // const vrrtc = window.document.getElementsByTagName('vr-rtc')[0] as VRRtcElement;
    // vrrtc.address = window.location.href.replace(/^http/, 'ws') + 'socket';
  })